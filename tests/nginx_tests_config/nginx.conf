worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;               # load MIME types
    default_type  application/octet-stream; # fallback media type
    sendfile       on;                      # use sendfile(2)

    # Load all virtual‑host configs
    include /etc/nginx/conf.d/*.conf;
}
